package 万达;

import java.util.Arrays;
public class Ary {
	String[] str = new String[1];
	int []   in = new int[0];
	public String [] Ar(String[] str){
		this.str = Arrays.copyOf(str, str.length);
		return str;		
	}
	public int [] Ar(int[] in){
		this.in = Arrays.copyOf(in, in.length);
		return in;
		
	}
}
